**Summary**
This is a simple project done in React, created from `create-react-app`.

**Setup**

- Run `npm install` 
  - This will download all the dependencies required for this project.
- Run `npm start`
  - This will start a server at the default port 3000 and the app will run at: http://localhost:3000

That's all :)